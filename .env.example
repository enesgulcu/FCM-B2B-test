# Copy to .env and fill in real values. Never commit .env.

DATABASE_URL=
NEXTAUTH_SECRET=
NEXTAUTH_URL=
NEXT_PUBLIC_API_URL=

# Server-only mail credentials (do NOT use NEXT_PUBLIC_ prefix)
EMAIL=
EMAIL_PASSWORD=
